
{% extends 'default_frame.twig' %}
{% set body_class = 'cart_page' %}
{% block main %}
<div class="row">
  <div class="col-sm-10 col-sm-offset-1">
    <h1 class="page-header">ログイン</h1>
    <form method="post" action="{{ url('login_check') }}">
      <input type="hidden" name="_target_path" value="shopping"/>
      <input type="hidden" name="_failure_path" value="shopping_login"/>
      <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}"/>
      <div class="row">
        <div class="col-sm-6">
          <div class="form-group">{{ form_widget(form.login_email, {attr: {placeholder: 'メールアドレス'}}) }}
            <div class="checkbox">
              <label>
                <input type="checkbox"/>メールアドレスを記憶
              </label>
            </div>
          </div>
          <div class="form-group">{{ form_widget(form.login_pass, {attr: {placeholder: 'パスワード'}}) }}</div>{% if error %}
          <div class="form-group"><span class="text-danger">{{ error.message|trans }}</span></div>{% endif %}
          <p>
            <button type="submit" class="btn btn-info btn-block btn-lg">ログイン</button>
            <ul>
              <li><a href="{{ url('forgot') }}">ログイン情報をお忘れですか？</a></li>
              <li><a href="{{ url('entry') }}">新規会員登録</a></li>
            </ul>
          </p>
        </div>
        <div class="col-sm-5 col-sm-offset-1">
          <p>会員登録をせずに購入手続きをされたい方は、下記よりお進みください。</p>
          <p><a href="{{ url('shopping_nonmember') }}" class="btn btn-info btn-block btn-lg">ゲスト購入</a></p>
        </div>
      </div>
    </form>
  </div>
</div>{% endblock %}