
<form method="get" action="{{ path('product_list') }}" class="navbar-form navbar-right">
  <div class="form-group">
    <label><i class="glyphicon glyphicon-search"></i> 商品を検索：</label>{{ form_widget(form.category_id) }}
  </div>
  <div class="form-group">{{ form_widget(form.name, {'attr': { 'placeholder' : "キーワードを入力" }} ) }}</div>
  <button type="submit" class="btn btn-default">検索</button>
</form>