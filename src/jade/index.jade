| {% extends 'default_frame.twig' %}

| {% set body_class = 'front_page' %}

| {% block javascript %}
| {% endblock %}

| {% block main %}

#top-carousel.carousel.slide(data-ride='carousel')
  ol.carousel-indicators
    li.active(data-target="#top-carousel" data-slide-to="0")
    li(data-target="#top-carousel" data-slide-to="1")
    li(data-target="#top-carousel" data-slide-to="2")
  .carousel-inner(role="listbox")
    .item.active
      img(width="100%" src="{{ app.config.front_urlpath }}/img/top/mv01.jpg")
    .item
      img(width="100%" src="{{ app.config.front_urlpath }}/img/top/mv02.jpg")
    .item
      img(width="100%" src="{{ app.config.front_urlpath }}/img/top/mv03.jpg")
  a.left.carousel-control(href="#top-carousel" role="button" data-slide="prev")
    span.glyphicon.glyphicon-chevron-left(aria-hidden="true")
    span.sr-only
      | Previous
  a.right.carousel-control(href="#top-carousel" role="button" data-slide="next")
    span.glyphicon.glyphicon-chevron-right(aria-hidden="true")
    span.sr-only
      | Next

| {% endblock %}
